interface:
  display_name: "Paper Package Runner"
  short_description: "Portable entry skill for paper-package generation"
  default_prompt: "Use $paper-package-runner to bootstrap the Paperer skills if needed and generate a paper package from a readable paper PDF. Default target_language to Chinese unless the user explicitly requests another language."
