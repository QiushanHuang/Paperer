interface:
  display_name: "Paper Package Runner"
  short_description: "Primary Paperer entry skill for normal paper-package generation"
  default_prompt: "Use $paper-package-runner as the primary Paperer entry skill. If the current workspace does not already contain the Paperer production skills, first obtain only the minimal `paperer-skill-package/` directory, not the full repo. Then generate a paper package from a readable paper PDF, defaulting target_language to Chinese unless the user explicitly requests another language."
